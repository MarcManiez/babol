extends layout.pug

block content
  main(class="HomePage")
    p Sharing music with other streaming services is hard. Share a Babol link instead.
    form(class="link-form", action="#")
      input(name="link", placeholder="Enter a music link", autofocus autocomplete="off")

